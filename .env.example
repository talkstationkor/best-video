# SQLite file used for local/dev. For a shared/production deployment,
# point this at a Postgres/MySQL connection string instead and change
# the `provider` in prisma/schema.prisma to match — no application code
# needs to change.
DATABASE_URL="file:./dev.db"
